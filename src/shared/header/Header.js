import NavLinks from '../nav/NavLinks';

function Header(){
    return(
        <header>
            <h1>Header</h1>
            <NavLinks classnames="nav_links"/>
        </header>
    );
}

export default Header;